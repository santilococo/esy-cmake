{
  "name": "esy-cmake",
  "version": "0.1.0",
  "description": "CMake package for esy",
  "license": "MIT",
  "esy": {
    "build": [
        ["./cmake/bootstrap", "--prefix=#{self.install}"],
        ["make", "-C", "cmake"],
        ["make", "-C", "cmake",  "install"]
    ]
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
