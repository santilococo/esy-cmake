{
  "name": "esy-cmake",
  "version": "0.1.0",
  "description": "CMake package for esy",
  "license": "MIT",
  "esy": {
    "build": [
        ["make", "#{os == 'windows' ? 'cmake-win32' : 'cmake'}", "PREFIX=#{self.install}", "BUILDDIR=#{self.target_dir}"]
    ]
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
