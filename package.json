{
  "name": "@reveryui/esy-cmake",
  "version": "0.3.5001",
  "description": "CMake package for esy",
  "license": "MIT",
  "esy": {
    "build": [
        ["bash", "-c",  "#{os == 'windows' ? './build-windows.sh' : './build.sh'}"],
        ["ls", "-a", "#{self.bin}"],
        ["cmake", "--version"]
    ],
    "buildsInSource": "_build"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
